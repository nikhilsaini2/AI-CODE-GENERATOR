{
  "name": "ai-code-editor-api",
  "version": "1.0.0",
  "description": "AI Code Editor Generator API",
  "main": "api/generate.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "cd frontend && npm run build"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
